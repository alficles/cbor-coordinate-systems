# OS cruft
.DS_Store

# xml2rfc / mmark build artifacts (regenerable via `make`)
gen/*.xml
gen/*.txt
gen/*.html
lib/
.refcache/
*.exp

# Windows `2>nul` redirect artifact when `make clean` runs on Unix
nul
